# KMS

## 목적

이 문서는 `왕사들(WANGSADEUL)` 프로젝트의 운영 기준 문서다.
다른 사람이나 다른 AI가 코드를 직접 열지 않아도 현재 사이트의 기능 구성, 화면 디자인 기준, 운영 규칙을 한 곳에서 확인할 수 있어야 한다.

## KMS 구조 원칙

KMS는 **두 개의 탭**으로 구성된다.

1. `기능정의서` 탭 — KMS의 제1 기능. 진입 시 기본으로 보이는 탭.
2. `디자인` 탭 — 화면 작업의 기준 문서.

관리자 페이지에서 `KMS`를 누르면 `기능정의서` 탭이 먼저 열린다.
운영 규칙(AI 작업 원칙, 배포 검토 원칙, 우선순위 등)은 KMS 화면의 탭으로 두지 않고, 이 문서 본문(`부록`)과 관리자 `버전 기록` 탭에서 관리한다.

## KMS 제1 원칙

KMS의 제1 기능은 `기능정의서`다.
즉, 이 문서는 다른 개발자가 코드를 직접 열지 않아도 현재 홈페이지가 어떤 기능을 갖고 있고, 각 기능이 어디까지 구현되어 있으며, 무엇이 아직 남아 있는지 먼저 이해할 수 있어야 한다.

기능정의서는 사이트가 존재하는 5가지 미션을 기준으로 구성한다.

1. 왕사들 커뮤니티 운영
2. 뱅기노자 강연 일정 안내
3. 뱅기노자 칼럼 공유
4. 뱅기노자 투어 프로그램 판매·운영
5. 뱅기노자 책 판매

이 5개 미션 위에 공통 기반(홈, 인증/계정, 마이페이지, 관리자, 운영 문서)이 받쳐준다.

기능정의서는 `실제 라우트`, `실제 노출 콘텐츠`, `실제 저장소 구조`를 기준으로 작성한다.
기획상 있었지만 화면에서 제거된 영역은 현재 기능으로 기록하지 않는다.

## 문서 운영 원칙

1. `kms.md`와 `ai-development-rules.md`는 같이 관리한다.
2. 새 규칙이 추가되면 두 문서를 동시에 갱신한다.
3. 구현 현황이 바뀌면 KMS도 함께 갱신한다.
4. 관리자 페이지의 KMS 화면은 이 문서와 같은 기준을 반영해야 한다.
5. KMS를 수정할 때는 무엇이 바뀌었는지만 적지 말고, 왜 수정했는지 계기와 배경도 함께 기록한다.
6. KMS 기록은 나중에 다른 AI나 사람이 보고도 맥락을 이해할 수 있어야 한다.

# 탭 1 · 기능정의서

이 섹션은 관리자 페이지 KMS의 `기능정의서` 탭과 같은 내용을 다룬다.
각 영역은 아래 구조로 정리한다.

- 영역 정체성(역할 / 라우트 / 상태)
- 현재 평가
- 없는 기능 정리(완성도를 높이려면 필요한 것)
- 기능(각 기능: 요소 / 기술 스펙 / 유의할 점 / 개발 이슈)
- 영역 차원의 기술 스펙
- 영역 차원의 유의할 점
- 영역 차원의 개발 이슈

## 5가지 미션 평가 요약

| # | 미션 | 상태 | 평가 |
|---|------|------|------|
| 01 | 왕사들 커뮤니티 | Cycle 1 마무리 (기능 ~85%) | 좋아요·북마크·신고·댓글 알림·등급 배지·페이지네이션을 도입해 사회적 신호를 갖췄음. 답글 트리·외부 스토리지·외부 DB만 남음 |
| 02 | 뱅기노자 강연 일정 안내 | Cycle 3 마무리 (기능 ~70%) | 회원 전용 신청·무통장 입금·관리자 입금 확인 → 참가 확정·정원/대기열·.ics·내 신청 내역까지 닫음. PG 결제·D-1 알림·자료 보관함은 다음 단계 |
| 03 | 뱅기노자 칼럼 공유 | Cycle 2 마무리 (기능 ~80%) | 임시저장·예약발행·좋아요·공유 링크·댓글·검색·아카이브·추정 읽기 시간 자동 계산 도입. 남은 큰 항목은 RSS·구독·작성자 프로필 |
| 04 | 뱅기노자 투어 프로그램 판매·운영 | Cycle 5 마무리 (기능 ~70%) | 회원 전용 신청·무통장 입금·관리자 입금 확인·정원/대기열·.ics·내 답사 내역 닫음. 강연·책과 같은 결제·알림 인프라 공유 |
| 05 | 뱅기노자 책 판매 | Cycle 4 마무리 (기능 ~65%) | 회원 전용·무통장 입금 단일 흐름으로 주문→입금→발송→배송 완료 사이클 닫음. 강연과 계좌 공유. PG·재고·영수증·환불은 다음 단계 |

## 영역 00 · 공통 기반 (BASE)

- 역할: 5개 미션이 공통으로 의지하는 진입점, 인증, 운영자 콘솔, 운영 문서.
- 라우트: `home`, `login / signup`, `mypage`, `admin`, `documents`
- 상태: 기본 구현
- 현재 평가: 랜딩 → 가입/로그인 → 마이페이지 → 관리자 콘솔까지의 뼈대는 모두 살아 있다. 다만 회원 식별 후 무엇을 할 수 있는지(주문·예약·구독)가 비어 있어 사용자에게 '계정의 의미'가 약하다.

### 없는 기능 / 완성도를 높이려면 필요한 것

- 외부 DB / 서버 인증으로의 전환(현재 local-first)
- 이메일 인증 · 비밀번호 재설정 · 소셜 로그인
- 마이페이지 프로필 수정 · 비밀번호 변경
- 히어로 통계(2,847 회원 등)의 실수치 연결
- 검색 · 전역 알림 센터

### 기능

#### 홈 랜딩 — 구현됨

- 요소: 히어로(일월오봉도 SVG, 슬로건, CTA, 레이아웃 토글 center/split/fullbleed) / 공지사항(상위 2건 강조 + 행 리스트) / 왕사남 강연 일정(3열) / 투어 프로그램(2열) / 뱅기노자 칼럼(피처 1 + 사이드 4) / 파트너십(3열) / 책 구매 CTA / 푸터 배포 버전 카드
- 기술 스펙: `HomePage` 단일 컴포넌트. 데이터는 `WANGSADEUL_DATA` 정적 + `WSD_STORES.userColumns` 병합. 레이아웃은 `tweaks.heroLayout`으로 토글.
- 유의할 점: 히어로 통계 수치는 하드코딩이라 실제 운영 수치와 어긋날 수 있음. 운영 화면(대시보드)과 동기화하기 전에는 '데모'로 봐야 함.
- 개발 이슈: fullbleed 모드에서 일월오봉도 SVG가 본문 가독성을 해치는 케이스 → radial-gradient 마스크로 완화.

#### 인증 / 계정 — 부분 구현

- 요소: 로그인/회원가입 토글 / 현재 인증 방식 안내 카드 / 약관·개인정보 동의 / 관리자 임시 계정(`admin@admin.admin / admin`) / 비밀번호 해시 저장 / 세션 유지 / 내비게이션 로그아웃
- 기술 스펙: `WSD_AUTH` helper + `WSD_STORES.users` / `WSD_STORES.session` localStorage. 비밀번호는 브라우저 내 해시.
- 유의할 점: local-first 인증이라 정적 배포 위에서만 동작. 외부 DB 연동 시 저장소만 교체하는 방향으로 설계되었으므로 계층 분리를 깨지 말 것.
- 개발 이슈: P1 초기에는 화면에서 즉석 user 객체만 만드는 수준이었음 → `WSD_AUTH` / `WSD_DB` 분리로 통합. Babel standalone + React UMD 환경이라 ESM import가 막혀 Tiptap·해시 라이브러리는 window 글로벌로 주입.

#### 마이페이지 — 부분 구현

- 요소: 비로그인 안내 / 계정 카드(이메일·등급·권한·가입 시각) / 등급·혜택 / 예정 강연 / 예정 투어 / 주문 상태 / 최근 커뮤니티 활동
- 기술 스펙: `MyPage` 단일 컴포넌트. `user` 세션 + `WSD_STORES.grades` + `WSD_COMMUNITY.listPosts()` + `WANGSADEUL_DATA.lectures/tours` + `cart` 상태.
- 유의할 점: 예정 강연/투어가 사용자 신청 내역이 아니라 사이트 다음 일정이라 '내가 신청한 것처럼' 보일 수 있음. 신청 흐름이 붙기 전까지 라벨링 주의.
- 개발 이슈: 주문/예약 저장소가 없어 마이페이지에 진짜 보여줄 데이터가 거의 없음.

#### 관리자 콘솔 — 부분 구현

- 요소: 사이드바 6개 그룹(요약/콘텐츠/회원·주문/운영 설정/개인정보/시스템) / 대시보드(실수치 4개 + 최근 게시글·칼럼 + 빠른 이동) / 게시글 관리(검색·필터·CSV·삭제) / 칼럼·칼럼 작성(Tiptap) / 투어·회원·주문 / 카테고리·회원 등급 / 개인정보 8개 탭(GDPR/PIPA) / 버전 기록 / KMS / 설정
- 기술 스펙: `AdminPage` 단일 컴포넌트. `WSD_COMMUNITY` / `WSD_AUTH` / `WSD_STORES` / `WANGSADEUL_DATA` / `PRIVACY_DATA` 동시 참조. 비관리자는 `AdminDenied`.
- 유의할 점: 관리자 콘솔이 단일 컴포넌트라 1900줄을 넘는다. 새 탭 추가 시 분할을 고려할 것.
- 개발 이슈: P1까지는 관리자 게시글 탭이 사용자 게시판과 다른 mock 배열을 봤음 → P2에서 `WSD_COMMUNITY`로 통합. Tiptap이 ESM으로만 제공되어 첫 마운트 전 `wsd-tiptap-ready` 이벤트 대기 fallback 필요.

#### 운영 문서 / KMS / 버전 기록 — 구현됨

- 요소: `kms.md` / `ai-development-rules.md` / `project-priority-table.md` / 관리자 KMS 탭(기능정의서+디자인) / 관리자 버전 기록 / 푸터 배포 버전 카드
- 기술 스펙: 정적 마크다운 + 관리자 화면이 같은 내용을 컴포넌트로 표시. `window.WSD_VERSION`이 푸터·관리자 빌드 표시의 단일 출처.
- 유의할 점: 문서와 화면이 어긋나면 다음 작업자가 혼선을 일으킨다. KMS 화면 = `kms.md` 본문 = 같은 기준으로 동기화 유지.

### 영역 차원 · 기술 스펙

프론트 단일 SPA(React UMD + Babel standalone) + localStorage 기반 저장소(`WSD_STORES`) + helper 계층(`WSD_AUTH`, `WSD_COMMUNITY`, `WSD_SAVE`). 외부 DB 연동 시 helper는 유지하고 저장소 구현만 교체하는 구조.

### 영역 차원 · 유의할 점

- 정적 배포(GitHub Pages) 환경이라 서버 측 권한 검증이 없으므로, 모든 권한 검사가 클라이언트 단에 그친다는 점을 잊지 말 것.
- Babel standalone로 JSX를 런타임 컴파일하므로 첫 페인트가 느릴 수 있다. 본격 트래픽 단계에서는 빌드 파이프라인 도입이 필요.

### 영역 차원 · 개발 이슈

- 같은 mock 데이터가 여러 곳에 흩어져 있던 P1 초기 → 단일 helper로 수렴.
- 정적 배포 환경에서 ESM 라이브러리 로딩이 깨지기 쉬워, window 글로벌 + ready 이벤트 패턴으로 고정.

## 영역 01 · 미션 1 — 왕사들 커뮤니티

- 역할: 회원이 질문·후기·정보를 남기고 운영자가 같은 흐름에서 관리하는 핵심 참여 영역.
- 라우트: `community`, `mypage(최근 활동)`, `admin > 게시글`
- 상태: P2 1차 가동(부분 구현)
- 현재 평가: 사용자 화면과 관리자 화면이 같은 `communityPosts` 저장소를 보는 데까지 정리되어 게시판 흐름은 작동한다. 그러나 좋아요/신고/알림/등급 가시화처럼 회원이 '커뮤니티답다'고 느낄 사회적 신호가 없어, 외부에서 봤을 때 단순 게시판처럼 보인다.

### 없는 기능

- 좋아요 · 공감 · 북마크
- 신고 · 차단 · 블랙리스트 운영 큐
- 댓글 답글(트리 구조) · 멘션 · 댓글 알림
- 회원 등급/배지 가시화(작성자 프로필 카드)
- 해시태그 / 인기글 / 주간 트렌드
- 이미지 외부 스토리지 업로드 (현재 base64 in-localStorage)
- 검색 결과 정렬 · 페이지네이션 · 기간 필터
- 본문 검색(현재는 제목·작성자 부분 일치만)
- 회원 활동 요약(글 수, 댓글 수, 활동 기간)

### 기능

#### 게시글 목록 / 검색 / 카테고리 필터 — 부분 구현

- 요소: 검색 입력(제목·작성자) / 카테고리 탭 / 정렬 영역 / 카드·행 리스트 / 페이지네이션(미구현)
- 기술 스펙: `WSD_COMMUNITY.listPosts()` → `WSD_STORES.communityPosts` localStorage. 카테고리 정의는 `WSD_STORES.categories` 중 `boardType === 'community'`.
- 유의할 점: 검색은 본문 검색이 빠져 있고 정렬은 최신순뿐. 데이터가 늘어나면 페이지네이션부터.
- 개발 이슈: 사용자 작성 글과 시드 글이 다른 키에 저장되어 있던 P1 → `ensureCommunityPostsSeeded`로 단일 키 통합.

#### 게시글 작성 / 수정 / 삭제 — 구현됨

- 요소: Tiptap 본문 에디터(StarterKit + Image + Link + Typography) / 카테고리 선택 / 이미지 첨부 / 임시 저장(미구현) / 수정·삭제 버튼
- 기술 스펙: `WSD_COMMUNITY.createPost / updatePost / deletePost`. 권한은 작성자 본인 혹은 `user.isAdmin`. Tiptap은 `window.WSD_TIPTAP`으로 ESM 주입.
- 유의할 점: 삭제는 즉시 영구 삭제. 운영 중 실수 방지를 위해 confirm() 한 번을 반드시 거치도록 유지.
- 개발 이슈: Tiptap이 첫 마운트보다 늦게 로드될 수 있어 `wsd-tiptap-ready` 이벤트 대기 fallback 추가. 임시 저장이 없어 작성 중 새로고침 시 본문 손실.

#### 댓글 등록 / 삭제 — 부분 구현

- 요소: 입력 폼 / 댓글 리스트 / 삭제 버튼 / 답글(미구현) / 멘션(미구현)
- 기술 스펙: `WSD_STORES.comments[postId]` 배열. push / filter.
- 유의할 점: 포스트 ID당 단일 배열이라 댓글 수가 많아지면 페이지네이션 구조 확장 필요.
- 개발 이슈: 게시글 `replies` 카운트와 실제 배열 길이가 어긋날 수 있어 normalize 시점에 동기화.

#### 조회수 저장 — 부분 구현

- 요소: 조회수 카드 / 본인 자동 카운트 방지(미구현) / 유닛 테스트(미구현)
- 기술 스펙: 상세 진입 시 `views += 1` 후 `WSD_SAVE.communityPosts()` 호출.
- 유의할 점: 동일 사용자 새로고침 시 중복 카운트 발생. 운영 수치 신뢰도가 낮음.

#### 이미지 첨부 — 부분 구현

- 요소: 드롭존 / Tiptap Image 확장 / Dropcursor
- 기술 스펙: 이미지를 base64로 인코딩해 본문 HTML에 직접 삽입. localStorage에 같이 저장됨.
- 유의할 점: 1~2MB 이미지 몇 개만 올려도 localStorage quota(5~10MB)에 빠르게 도달 → 외부 스토리지 필요.
- 개발 이슈: base64 저장으로 운영 중 quota 초과 케이스가 보고됨.

#### 카테고리 접근 제한 — 부분 구현

- 요소: 카테고리 정의(`boardType`, `requiresLogin`, `requiresAdmin`) / 게이트 컴포넌트
- 기술 스펙: `WSD_STORES.categories` 메타에 권한 플래그, 컴포넌트 단에서 검사.
- 유의할 점: 클라이언트 단 검사라 외부 DB 도입 시 서버 측 권한 정책 별도로 가져가야 함.

#### 관리자 게시글 운영 — 구현됨

- 요소: 검색 입력 / 분류 필터(카테고리) / CSV 다운로드 / 행 단위 열기·삭제
- 기술 스펙: `WSD_COMMUNITY.exportCsv()` + `WSD_COMMUNITY.deletePost(id)`. 사용자 화면과 동일 저장소.
- 유의할 점: 관리자 삭제는 즉시 사용자 화면에 반영되므로 confirm 필수.
- 개발 이슈: P1 시점에 관리자 탭이 mock 배열을 보던 문제 → P2에서 통합.

### 영역 차원 · 기술 스펙

`WSD_COMMUNITY` helper + `WSD_STORES.communityPosts / comments / categories` localStorage. 외부 DB 교체 시 helper는 유지하고 저장소 구현만 교체.

### 영역 차원 · 유의할 점

- localStorage 한계 → 이미지·대량 데이터 누적 시 quota 초과
- 권한 검사가 클라이언트 단 → 외부 DB 도입 시 서버 측 정책 필수
- 사용자 화면 ↔ 관리자 화면이 같은 저장소를 본다는 가정이 P2 통합의 핵심이므로 깨지지 않게 유지

### 영역 차원 · 개발 이슈

- 사용자 작성 글 / 시드 글이 다른 키에 저장되어 있던 P1 → `ensureCommunityPostsSeeded`로 마이그레이션
- 관리자와 사용자 화면이 다른 mock 배열을 보던 P1 → `WSD_COMMUNITY` 단일 helper로 수렴

## 영역 02 · 미션 2 — 뱅기노자 강연 일정 안내

- 역할: 공개 / 심화 / 현장 강연 일정을 알리고 신청을 유도.
- 라우트: `home(노출)`, `tour(상세 영역)`, `mypage(예정 강연)`
- 상태: 정보 노출만(부분 구현)
- 현재 평가: `data.lectures` 정적 데이터를 홈에 카드로 보여주는 것이 전부. '알리는' 단계에서 멈춰 있어 신청 → 결제 → 참가 → 후기로 이어지는 사이클이 비어 있다. 강연이 자산으로 누적되지 못한다.

### 없는 기능

- 강연 신청 폼(회차 · 인원 · 대표자)
- 정원 / 대기열 / 잔여석 실시간
- 유료 강연 결제 / 무료 강연 등록 분기
- 캘린더 다운로드(.ics) · 구글 캘린더 추가
- D-1 알림 · 변경 알림(이메일 / 푸시)
- 참가자 명단 · 체크인 · 출석 이력
- 강연 후 자료 보관함(영상 · PDF · 발표자료)
- 마이페이지 신청 내역 / 출석 이력
- 강연자 프로필 페이지 / 시리즈 묶음
- 강연 후기 / 평점
- 관리자 강연 운영 화면(현재는 투어 탭에 정보만)

### 기능

#### 홈 강연 일정 노출 — 구현됨

- 요소: 프로그램 라벨(왕사남) / 주제 / 강연자 / 장소 / 다음 일정 / 잔여석 텍스트
- 기술 스펙: `HomePage`에서 `data.lectures` map. 첫 카드 강조 + 클릭 시 `tour` 라우트 이동.
- 유의할 점: 잔여석은 정적 문자열이라 실제 신청 수와 동기화되지 않음.

#### 투어 페이지 강연 통합 노출 — 부분 구현

- 요소: 프로그램 헤더 / 강연·투어 혼합 카드
- 기술 스펙: `WangsanamTourPage` 단일 페이지에서 `data.lectures`와 `data.tours`를 같이 다룸.
- 유의할 점: 사용자가 별도 강연 라우트가 있다고 오해할 수 있음. 신청 흐름 도입 시점에 페이지 분리 결정 필요.
- 개발 이슈: 라우트가 `tour` 하나뿐인데 강연이 함께 노출되어, 마이페이지에서 강연 진입 동선이 어색.

#### 마이페이지 예정 강연 카드 — 부분 구현

- 요소: 다음 강연 카드(`data.lectures[0]`)
- 기술 스펙: `data.lectures[0]`을 그대로 표시. 사용자 신청 이력과 무관.
- 유의할 점: '내가 신청한 강연'으로 오해 가능. 라벨에 '다가오는 강연'임을 명확히 유지.
- 개발 이슈: 개인화가 없어 마이페이지 본질에 어긋난다는 한계 — 신청 흐름 도입 시 우선 교체 대상.

### 영역 차원 · 기술 스펙

`WANGSADEUL_DATA.lectures` 정적 배열에만 의존. 신청·결제·정원 helper 미존재. 강연 도메인 모델은 미정의.

### 영역 차원 · 유의할 점

- 강연이 신청 가능한 것처럼 보이지만 실제 등록 흐름이 없음 → 안내 카드에 '신청은 별도 채널' 같은 설명 권장
- 잔여석 등 운영 수치는 정적 문자열이라 운영자가 직접 갱신하지 않으면 어긋남

### 영역 차원 · 개발 이슈

- 강연이 별도 도메인 모델 없이 카탈로그 데이터로만 정의되어 있어, 신청 기능 추가 시 모델·저장소·관리자 화면을 동시에 설계해야 함
- 투어와 한 페이지에 묶여 있어 라우팅·정보 구조 결정이 후속 기능 설계의 전제

## 영역 03 · 미션 3 — 뱅기노자 칼럼 공유

- 역할: 뱅기노자의 글을 공개해 브랜드 신뢰와 깊이를 만드는 콘텐츠 영역.
- 라우트: `column(공개)`, `home(추천)`, `admin > 칼럼 / 칼럼 작성(운영)`
- 상태: 기본 구동(부분 구현)
- 현재 평가: 관리자 발행 → 공개 사이트 즉시 반영이라는 P1 핵심 연결은 완성. 다만 독자가 칼럼과 상호작용하는 흐름(공유·구독·북마크·검색)이 없어 콘텐츠가 일회성으로 소비된다. 시리즈/카테고리 인덱스가 없어 아카이브로의 가치도 낮음.

### 없는 기능

- 댓글 / 좋아요 / 공유
- 이메일 · 웹 푸시 구독, 신규 칼럼 알림
- 칼럼 검색 / 카테고리 아카이브 / 시리즈 인덱스
- 예약 발행 / 임시 저장 / 발행 취소
- 추정 읽기 시간 자동 계산(현재 수동 입력)
- 작성자 프로필 카드 · 관련 글 자동 추천
- 열람 통계 / 좋아요 통계 운영 화면
- RSS / Atom 피드

### 기능

#### 공개 칼럼 목록 — 부분 구현

- 요소: 피처 칼럼 1건 + 보조 칼럼 4건(홈) / 전체 카드 그리드(칼럼 페이지) / 카테고리 필터(미구현)
- 기술 스펙: `[...WSD_STORES.userColumns, ...WANGSADEUL_DATA.columns]` 병합. 사용자 발행 글이 위에 오도록 spread 순서 유지.
- 유의할 점: 정렬 기준이 명시적이지 않고 spread 순서에 의존 → 정책으로 명문화 필요.

#### 칼럼 상세 — 부분 구현

- 요소: 제목 / 카테고리 / 날짜 / 추정 읽기 시간 / 본문 HTML / 관련 글(미구현) / 공유·좋아요(미구현)
- 기술 스펙: 본문은 HTML 문자열 그대로 dangerouslySetInnerHTML.
- 유의할 점: 관리자가 임의 HTML/script를 넣을 수 있으므로 에디터 정책으로 차단. 사용자 입력에는 절대 적용하지 않기.
- 개발 이슈: Tiptap 본문이 HTML로 직렬화되어 저장되므로 어떤 확장이 활성화돼 있는지를 같이 관리해야 함.

#### 관리자 칼럼 작성 — 구현됨

- 요소: Tiptap StarterKit + Image + Link + Typography / 카테고리 입력 / 발행 버튼 / 임시 저장(미구현) / 예약 발행(미구현)
- 기술 스펙: `WSD_STORES.userColumns`에 push 후 `WSD_SAVE.userColumns()` 호출.
- 유의할 점: 발행이 곧 공개라 임시 저장이 없으면 작성 중 새로고침 시 본문 손실 위험.
- 개발 이슈: 수동 ID 부여 시 시드 칼럼과 ID 충돌 가능성 → 충돌 검사 필요.

#### 홈 추천 칼럼 — 구현됨

- 요소: 피처 카드 1 / 사이드 4건
- 기술 스펙: 공개 칼럼 병합 결과의 상위 항목을 그대로 사용.
- 유의할 점: 추천 알고리즘이 없어 항상 최신 5건이 노출됨.

### 영역 차원 · 기술 스펙

`WANGSADEUL_DATA.columns`(시드) + `WSD_STORES.userColumns`(관리자 발행) 병합. 본문은 Tiptap이 HTML로 직렬화한 문자열.

### 영역 차원 · 유의할 점

- 공개 칼럼 정렬 기준이 명시적이지 않다는 점 → 정책으로 명문화 필요
- 본문 HTML 신뢰 범위는 '관리자 입력에 한함'으로 유지(사용자 입력에 dangerouslySetInnerHTML 절대 금지)

### 영역 차원 · 개발 이슈

- Tiptap 확장 변경이 본문 저장 호환성에 영향을 주므로, 확장 추가/제거 시 기존 본문 호환성 테스트 필요
- 관리자 칼럼과 시드 칼럼 ID 충돌 가능성

## 영역 04 · 미션 4 — 뱅기노자 투어 프로그램 판매·운영

- 역할: 뱅기노자가 진행하는 궁궐 답사·역사 답사 프로그램을 판매하고 운영.
- 라우트: `tour(목록·상세)`, `home(노출)`, `admin > 투어(운영)`
- 상태: 카탈로그(부분 구현 — 판매 흐름 부재)
- 현재 평가: 5개 미션 중 가장 약한 영역. 카드형 카탈로그까지는 그려져 있지만 예약·결제·정원·환불·관리자 운영 어느 것도 동작하지 않는다. 사용자가 누르는 '예약' 버튼이 실제로는 아무 일도 일어나지 않아 운영자에게 잘못된 신호를 줄 수 있다.

### 없는 기능

- 투어 신청 폼(회차 · 인원 · 대표자 · 동행자)
- 결제 게이트웨이 연동
- 회차별 좌석 / 대기열 관리
- 참가자 명단 · 체크인
- 환불 · 취소 정책 · 환불 처리
- 관리자 예약 운영(승인 / 거절 / 메모)
- 마이페이지 예약 내역 · 이메일 영수증
- 외국어 안내(영문) 옵션
- 프로그램 후기 · 평점
- 이미지 갤러리(현재 카드 한 장)
- 지도 · 집결지 안내 · 우천 시 운영 정책

### 기능

#### 투어 목록 — 구현됨

- 요소: 난이도 배지 / 기간·인원·가격 / 다음 일정 / 프로그램 설명
- 기술 스펙: `WANGSADEUL_DATA.tours` 정적 배열을 `WangsanamTourPage`가 카드로 렌더.
- 유의할 점: 데이터가 정적이므로 다음 일정 갱신은 운영자가 직접 코드/문서로 반영해야 함.

#### 투어 상세 카드 — 부분 구현

- 요소: 기간·인원·난이도 / 다음 일정 / 가격 / 설명 / 이미지 갤러리(미구현) / 회차별 일정 캘린더(미구현)
- 기술 스펙: `WangsanamTourPage` 안에서 같은 카드를 사용.
- 유의할 점: 이미지 갤러리·일정 캘린더 부재로 의사결정에 필요한 정보가 부족함.

#### 예약 / 대기자 버튼 — UI만(미구현)

- 요소: 예약 버튼 / 대기자 버튼
- 기술 스펙: 클릭 핸들러가 비어 있거나 정적 알림만 노출.
- 유의할 점: 기능 없는 버튼이 노출되면 사용자 신뢰 저하. MVP 전까지는 '예약 문의' 형태로 안내.
- 개발 이슈: 사용자가 클릭 후 아무 일도 일어나지 않아 운영자에게 잘못된 '관심도' 신호를 줄 수 있음.

#### 관리자 투어 탭 — 부분 구현

- 요소: 투어 표(목록)
- 기술 스펙: 현재는 `data.tours` 동일 데이터를 표로 보여주는 수준.
- 유의할 점: 예약 데이터가 없으므로 운영 화면이 카탈로그와 같은 정보만 갖고 있음.

### 영역 차원 · 기술 스펙

`WANGSADEUL_DATA.tours` 정적 데이터에만 의존. 예약 / 결제 / 정원 / 명단 helper 미존재. 도메인 모델 미정의.

### 영역 차원 · 유의할 점

- '판매'라는 말이 작동하려면 결제 · 정원 · 환불 셋이 동시에 필요함을 잊지 말 것
- 결제 도입 전까지는 사용자에게 '문의 접수' 흐름임을 명확히 표시
- 강연과 한 페이지에 묶여 있다는 점이 신청 흐름 설계에 영향 → 페이지 분리 결정 선행

### 영역 차원 · 개발 이슈

- 예약 데이터가 없으므로 마이페이지·관리자에서 보여줄 진짜 데이터가 부재
- 투어와 강연이 같은 페이지를 공유 → 정보 구조 결정이 후속 기능 설계의 전제

## 영역 05 · 미션 5 — 뱅기노자 책 판매

- 역할: 뱅기노자의 책 『왕의길』을 소개하고 판매.
- 라우트: `book(상세)`, `checkout(주문)`, `home(CTA)`, `admin > 주문(운영)`
- 상태: UI 흐름까지(부분 구현)
- 현재 평가: 책 정보 → 판본·수량 → 장바구니 → 체크아웃 UI까지의 화면은 그려져 있지만 주문 저장 / 결제 / 배송 / 영수증 / 재고 / 환불 어느 것도 작동하지 않아 실제 판매가 불가능하다. 도메인 4(투어)와 같은 한계.

### 없는 기능

- 주문 저장소 / 주문 ID 발급
- 결제 게이트웨이 연동(KR / EN 가격 분기)
- 배송 정보 입력 · 송장 연동
- 영수증 / 세금계산서
- 재고 관리 · 품절 표시
- 주문 상태 추적(주문확인 / 결제완료 / 배송중 / 도착)
- 마이페이지 주문 내역 · 재구매
- 환불 · 교환 흐름
- 독자 리뷰 · 평점
- 교차 판매(투어 / 강연 패키지)
- 쿠폰 · 회원 등급 할인
- 장바구니 영속성(현재 메모리 휘발성)

### 기능

#### 책 상세 — 구현됨

- 요소: 표지 / 저자 / 출판사 / ISBN / 페이지 수 / 국문·영문 가격 / 챕터 목차 / 설명 본문
- 기술 스펙: `WANGSADEUL_DATA.book` 정적 객체를 `BookCheckoutPage`가 렌더.
- 유의할 점: ISBN과 가격은 정적이라 출판사 정책 변경 시 코드 갱신 필요.

#### 판본 · 수량 선택 — 부분 구현

- 요소: 판본 토글 / 수량 ± / 합계
- 기술 스펙: `React.useState`로 `cart` 메모리 상태 보관.
- 유의할 점: 장바구니가 메모리 상태라 새로고침 / 다른 페이지 → 돌아오기 시 사라짐. localStorage 영속화 필요.
- 개발 이슈: 사용자가 '담기 → 다른 페이지 → 돌아오기' 시 사라지는 케이스가 운영 검토에서 자주 보고됨.

#### 장바구니 — 부분 구현

- 요소: 선택 항목 카드 / 수량 변경 / 삭제 / 합계
- 기술 스펙: App 컴포넌트의 `cart` 상태를 prop drilling으로 전달.
- 유의할 점: Context / Reducer 도입 전까지는 prop chain이 길어지지 않게 페이지 단위 유지.

#### 체크아웃 UI — 부분 구현

- 요소: 주문 요약 / 배송지 폼 / 결제 폼 / 주문 완료 화면(미구현)
- 기술 스펙: 폼 UI만 존재. 제출 시 실제 처리 없음.
- 유의할 점: 결제 연동 시 PCI / 카드 정보 비저장 / PII 처리 정책이 같이 따라옴 → 개인정보(GDPR/PIPA) 모듈과 동기화 필요.
- 개발 이슈: 결제 도입 시 카드 정보 입력을 '직접' 받지 않도록 처음부터 게이트웨이 위임 구조로 설계할 것.

#### 마이페이지 주문 상태 — 부분 구현

- 요소: 주문 상태 카드
- 기술 스펙: `cart` 상태만 참조. 저장된 주문이 없어 항상 빈 상태 또는 mock.
- 유의할 점: 주문 저장소 도입 전까지는 카드 라벨에 '진행 중인 주문'임을 명시.

#### 관리자 주문 탭 — 미구현

- 요소: 주문 목록(미구현) / 주문 상세(미구현) / 환불·교환(미구현)
- 기술 스펙: 주문 저장소가 없어 화면 골격만 존재.
- 유의할 점: 주문 저장 도입 시 회원 등급/쿠폰 등 모듈과 동시에 설계해야 함.

### 영역 차원 · 기술 스펙

`WANGSADEUL_DATA.book` + `cart` 메모리 상태. 주문 / 결제 / 재고 / 환불 helper 미존재.

### 영역 차원 · 유의할 점

- 장바구니가 휘발성이라 사용자 흐름이 짧게만 유지됨 → localStorage 영속화가 1순위
- 국문/영문 가격이 분리되어 결제 게이트웨이가 통화별로 별도 계약될 수 있음
- 결제 도입 시 PCI/PII 책임이 발생 → 직접 카드정보를 받지 않는 게이트웨이 위임 구조로 설계

### 영역 차원 · 개발 이슈

- 결제·배송·재고·환불을 한 번에 도입하기 어려움 → 단계: 주문 저장 → 결제 위임 → 배송 → 영수증 → 재고 → 환불 순으로 점진 도입 권장

# 탭 2 · 디자인

이 섹션은 관리자 페이지 KMS의 `디자인` 탭과 같은 내용을 다룬다.
새 화면을 만들거나 기존 화면을 바꿀 때, 아래 원칙을 먼저 확인하고 그 위에서 작업한다.

## 1. 브랜드 무드

- 조선 왕실, 궁궐, 기록물, 전시 도록의 분위기를 기본으로 한다.
- 값싼 동양풍 장식보다 절제된 권위와 정적 긴장을 우선한다.
- 화면은 화려함보다 깊이와 밀도를 느끼게 해야 한다.

## 2. 컬러 원칙

- 기본 배경은 짙은 먹색 계열을 유지한다.
- 금색은 강조용 포인트로만 사용하고 남용하지 않는다.
- 주요 본문은 높은 가독성을 유지하는 밝은 중성 톤을 쓴다.
- 위험/삭제/경고 색상은 금색과 명확히 구분되게 유지한다.

## 3. 타이포그래피 원칙

- 제목은 세리프 중심으로 품격 있게 보이도록 유지한다.
- 본문은 읽기 쉬운 한글 폰트를 사용한다.
- 정보 라벨, 메타 정보, 운영 수치는 모노 계열을 사용해 구조를 분명하게 한다.
- 한 화면 안에서도 제목, 본문, 메타 정보의 역할 차이가 시각적으로 분명해야 한다.

## 4. 레이아웃 원칙

- 여백은 넉넉하게 두고, 답답하게 붙이지 않는다.
- 카드, 표, 섹션은 편집 디자인처럼 정렬감 있게 구성한다.
- 모바일에서도 구조가 무너지지 않도록 한 줄 정보량을 조절한다.
- 기능이 많아도 화면은 조용하고 차분하게 읽혀야 한다.

## 5. 컴포넌트 원칙

- 버튼은 역할이 분명해야 한다.
- 주요 행동은 금색 강조 버튼, 일반 행동은 기본 버튼, 위험 행동은 경고 색상 버튼으로 구분한다.
- 표와 카드의 역할이 겹치지 않게 사용한다.
- 배지, 필터, 상태 라벨은 운영 판단에 도움이 되도록 짧고 명확하게 만든다.

## 6. 인터랙션 원칙

- 애니메이션은 과하지 않게 사용한다.
- hover, focus, active 상태는 분명하되 시끄럽지 않게 표현한다.
- 관리자 화면은 장식보다 정보 탐색성과 작업 효율을 우선한다.
- 사용자 화면은 감성 전달과 신뢰감을 함께 주어야 한다.

## 7. 디자인 금지 원칙

- 밝은 흰색 바탕 중심의 일반 SaaS 느낌으로 바꾸지 않는다.
- 보라색 계열을 브랜드 주색처럼 사용하지 않는다.
- 과한 그라데이션과 유행형 마이크로 인터랙션을 남발하지 않는다.
- 기존 브랜드 분위기와 맞지 않는 귀여운 아이콘 중심 화면으로 흐르지 않게 한다.

# 부록 · 운영 기준

KMS 화면의 두 탭 외에, 다음 운영 기준은 이 문서 본문과 관리자 `버전 기록` 탭에서 함께 관리한다.

## 현재 우선순위 기준

기준 문서: `project-priority-table.md`

1. 인증/권한
2. 데이터 저장 구조
3. 관리자 발행물과 공개 페이지 연결
4. 커뮤니티 실서비스화
5. 관리자 운영 기능 고도화
6. 투어 예약
7. 책 주문/결제
8. 부가 기능

## P1 진행 상태

현재 기준으로 P1은 `현재 정적 배포 구조 안에서 완료` 상태다.

### 현재까지 완료된 P1

1. 인증/권한 — `WSD_AUTH` 기준으로 로그인, 회원가입, 로그아웃, 세션 유지가 한 구조에서 동작
2. 관리자 발행물과 공개 페이지 연결 — 관리자 발행 칼럼이 공개 칼럼 페이지와 홈 화면에 반영
3. 데이터 저장 구조 — `WSD_DB`, `WSD_STORES`, `WSD_SAVE` 기준으로 회원, 세션, 게시글, 댓글, 칼럼, 권한 엔티티 구조 분리

## P2 진행 상태

현재 기준으로 P2는 `진행 중` 상태다.

### 현재까지 진행된 P2

1. 커뮤니티 실서비스화 — `communityPosts` 단일 저장소, 게시글 작성/수정/삭제, 댓글 등록/삭제, 조회수 저장을 같은 helper 계층으로 정리
2. 관리자 운영 기능 고도화 — 관리자 게시글 탭이 실제 커뮤니티 저장소를 읽고 검색·필터·CSV·삭제가 실데이터 기준으로 동작

## AI 작업 기본 원칙

1. 모든 AI는 작업 시작 전에 `ai-development-rules.md`를 먼저 읽는다.
2. 모든 AI는 `project-priority-table.md`를 확인하고 우선순위 안에서 작업한다.
3. 모든 수정사항은 가능하면 푸시와 배포까지 진행하여 실제 검토 가능 상태로 만든다.
4. 설명이 필요하면 비개발자도 이해할 수 있는 표현을 사용한다.
5. 계획이 필요한 작업은 문서로 만들고, 완료 후 삭제한다.

## 버전 관리 원칙

버전 형식은 `AA.BBB.CCC`를 사용한다.

1. `AA` — 대버전(사용자가 직접 판단해서 올린다)
2. `BBB` — 기능 추가 또는 삭제 시 올린다
3. `CCC` — 작은 수정, 버그 수정, 문구 수정, 마이너 개선 시 올린다

## 문서 동기화 규칙

아래 항목이 바뀌면 `kms.md`도 함께 갱신한다.

1. 개발 규칙
2. 우선순위
3. 버전 체계
4. 구현 현황
5. 디자인 테마
6. 관리자 페이지 운영 방식

## KMS 기록 방식

KMS를 수정할 때는 가능하면 아래 구조를 따른다.

1. 무엇이 바뀌었는가
2. 왜 바꾸게 되었는가
3. 어떤 배경과 맥락이 있었는가
4. 이후 어떤 작업과 연결되는가

# 변경 기록

- 2026-04-25: KMS 문서 최초 생성
- 2026-04-25: AI 선독 원칙, 비개발자 설명 원칙, 계획 문서 원칙, 버전 체계, 관리자 버전 기록 원칙 반영
- 2026-04-25: KMS 수정 시 변경 계기와 배경을 함께 기록하는 원칙 추가
- 2026-04-25: 관리자 페이지 KMS 및 버전 기록 UI 반영 현황 업데이트
- 2026-04-25: 푸터 버전 표시 강화 및 관리자 발행 칼럼 공개 연결 현황 업데이트
- 2026-04-25: 사이트 전체 기능 인벤토리 섹션 추가
- 2026-04-25: P1 부분 완료 상태와 남은 항목 구분 기록 추가
- 2026-04-25: P1 local-first 인증/데이터 저장 구조 완료 및 P2 진입 가능 상태 반영
- 2026-04-25: P2 커뮤니티 저장소 통합 및 관리자 게시글 운영 기능 1차 반영
- 2026-04-25: KMS 기능정의서 우선 구조 및 디자인 원칙서 추가
- 2026-04-25: 대시보드 실데이터 연결, 왕사남 소개 제거, KMS 내부 기능정의서/디자인 탭 구조 반영
- 2026-04-25: KMS 화면을 `기능정의서` + `디자인` 두 탭 구조로 정리하고 진입 기본 탭을 `기능정의서`로 고정. 운영 원칙은 부록과 버전 기록으로 이동.
- 2026-04-25: 기능정의서를 사이트의 5가지 미션(커뮤니티 / 강연 / 칼럼 / 투어 / 책) + 공통 기반(BASE) 영역 단위로 재정렬. 각 영역마다 평가, 없는 기능 정리, 기능별 요소·기술 스펙·유의할 점·개발 이슈, 영역 차원 기술 스펙·유의할 점·개발 이슈를 표준 9블록 구조로 작성. 관리자 KMS 화면에는 우측 스티키 목차(TOC)를 추가해 영역 간 이동을 빠르게 만듦.
- 2026-04-25: Cycle 1(왕사들 커뮤니티 마무리) 출시. 좋아요·북마크·신고 운영 큐·댓글 알림·작성자 등급 배지·페이지네이션을 한 PR에 묶어 도입. `WSD_STORES`에 `bookmarks` / `reports` / `notifications` 신설, `AuthorGradeBadge` / `NotificationBell` 공통 컴포넌트 추가, 관리자 콘텐츠 메뉴에 `신고` 탭 신설. 미션 1 커버리지가 70% → ~85%로 상승.
- 2026-04-25: Cycle 2(뱅기노자 칼럼 운영 강화) 출시. 임시 저장·예약 발행·발행 취소 + 좋아요·공유 링크·댓글·검색·카테고리 아카이브·추정 읽기 시간 자동 계산 도입. `WSD_COLUMNS` helper와 `WSD_STORES.columnEngagement` 신설, App에 URL 해시 딥 링크(`#col-{id}` / `#post-{id}`) 추가. 미션 3 커버리지가 50% → ~80%로 상승.
- 2026-04-25: Cycle 3(뱅기노자 강연 운영) 출시. 회원 전용 강연 신청, 무통장 입금 결제(PG 도입 전 임시), 관리자 입금 확인 → 참가 확정, 정원·대기열 자동 처리, .ics 캘린더 다운로드, 마이페이지 내 신청 내역, 관리자 강연 탭과 계좌번호 설정 탭을 한 PR에 묶어 도입. `WSD_LECTURES` helper와 `WSD_STORES.lectureOverrides / lectureRegistrations / bankAccount` 신설, `lectures` 라우트 + `#lecture-{id}` 해시 추가. 미션 2 커버리지가 25% → ~70%로 상승.
- 2026-04-25: UX 개선 묶음 출시. 관리자 버전 기록 10건/페이지 + 총 개수 요약, 우하단 '맨 위로' 플로팅 버튼, 내비 `커뮤니티` hover 시 게시판 서브메뉴, 카테고리 관리에서 게시판 제목·설명 인라인 편집, `고딕 / 명조` 토글이 사이트 전체(헤더·푸터·카드·홈·강연 등) 본문 폰트에 적용되도록 CSS 변수 단위 확장.
- 2026-04-25: Cycle 4(뱅기노자 책 판매) 출시. 회원 전용 무통장 입금 단일 흐름으로 책 주문 → 입금 → 발송 → 배송 완료 사이클 도입. `WSD_BOOK_ORDERS` helper와 `WSD_STORES.bookOrders` 신설, 강연과 같은 `bankAccount` 저장소 공유. 관리자 사이드바 메뉴 명칭을 홈페이지 내비와 일치시킴(커뮤니티 / 강연 / 투어 프로그램 / 뱅기노자 칼럼 / 왕의길). 미션 5 커버리지가 25% → ~65%로 상승.
- 2026-04-25: Cycle 5(뱅기노자 투어 판매·운영) + 공통 인프라 강화 출시. `WSD_TOURS` helper와 `WSD_STORES.tourOverrides / tourReservations` 신설, 회원 전용 답사 신청 → 무통장 입금 → 관리자 입금 확인 → 참가 확정 사이클이 강연·책과 같은 패턴으로 닫힘. 통합 알림 인프라 도입(강연/책/투어 상태 변경 시 본인에게 자동 알림 + 헤더 ◇ 벨이 알림 타입별로 라우팅). 장바구니 localStorage 영속화로 새로고침에도 유지. URL 해시 `#tour-{id}` 추가. 미션 4 커버리지가 20% → ~70%로 상승.
- 2026-04-26: 운영 인프라 묶음 출시. (1) `MemberAdminPanel` 신설로 실제 등록 회원 목록·검색·등급 변경·관리자 권한 토글·정지/해제·삭제·활동 이력을 한 화면에서 처리. (2) `AdminCategoryPanel` 개선 — 카드형 추가 + 순서 변경 + 게시판별 글 수 + 등급×게시판 권한 매트릭스 뷰. (3) `WSD_LEGAL` / `WSD_FAQ` + `LegalAdminPanel` / `FaqAdminPanel` + 공개 페이지 `privacy` / `terms` / `faq` 라우트 추가, 푸터 버튼 연결. (4) `LecturesPage`를 `TourPage`와 동일한 탭+스티키 사이드바 UI로 재구조. (5) `TourPage` 하단에 `TourReviewsSection` 도입(참가 확정 회원만 작성, 평균 평점). (6) `CommunityPage` 상단에 `MY ACCESS` 배너로 현재 등급·읽기/쓰기 가능 게시판 수 표시.
- 2026-04-26: 기능 정상화 묶음 출시. (1) 댓글 답글 트리(`CommentTree`) — 커뮤니티/칼럼 1단계 답글. (2) 강연 후기 섹션(`LectureReviewsSection`) — 투어 후기와 동일 패턴, 참가 확정 회원만 별점·후기. (3) 강연/투어 신규 등록 — 관리자 패널에 `+ 새 강연/투어 추가` 버튼 + 카드 헤더 삭제 버튼. (4) 주문 영수증 텍스트 다운로드(`WSD_BOOK_ORDERS.generateReceipt / downloadReceipt`) — 마이페이지·관리자에서 발급. (5) 운영 감사 로그(`WSD_AUDIT` + `AuditLogPanel`) — 회원·강연·투어·책 핵심 액션 자동 기록, 검색/CSV/삭제 가능. (6) 활동 기반 자동 등급 승격(`WSD_GRADE_PROMO`) — 댓글 5+로 독자, 글 3+ 댓글 15+로 사관 자동 승격, 본인 알림 + 감사 로그.
