name = "banginoja-api"
main = "src/index.js"
compatibility_date = "2025-01-01"
account_id = "b8cc5609b10c4df5b8e31aec570937b8"

# v00.295.004 — 커스텀 도메인. 사이트(bgnj.net)와 같은 사이트로 만들기 위한 것이다.
# workers.dev 는 bgnj.net 과 다른 사이트라 세션 쿠키가 third-party 쿠키가 되고,
# Safari 는 그걸 전면 차단한다(2026-08-21 실제 장애 — handoff 15번).
# api.bgnj.net 이면 same-site 라 쿠키가 정상 동작한다.
# workers.dev 주소도 계속 살아 있다 — 옛 코드를 문 브라우저가 갑자기 끊기지 않게.
# ⚠ workers_dev 를 명시하지 않으면 wrangler 가 workers.dev 주소를 꺼 버린다.
# 실제로 이 줄이 없어서 배포 직후 옛 주소가 404 가 되며 사이트가 끊겼다(2026-08-21, 약 1분).
# 옛 주소를 문 브라우저가 남아 있는 한 절대 지우면 안 된다.
workers_dev = true

[[routes]]
pattern = "api.bgnj.net"
custom_domain = true

[[d1_databases]]
binding = "DB"
database_name = "banginoja-db"
database_id = "f9974cb8-c5f9-4033-9cf2-b596098b677b"

[[r2_buckets]]
binding = "MEDIA"
bucket_name = "banginoja-media"

[vars]
# localhost / 127.0.0.1 의 임의 포트는 코드 측에서 자동 허용(isLocalDevOrigin).
# v00.109 — HTTP origin 제거 (보안 audit MEDIUM). HTTPS 만 허용.
# 옛 http:// 항목은 SSL 미도입 시기 임시 허용이었음 (v00.064~v00.108). 이제 SSL 활성화 후 차단.
# 만약 SSL 미도입 환경 (예: 로컬 IP 직접 접근) 이면 isLocalDevOrigin 로직이 자동 허용.
ALLOWED_ORIGINS = "https://bgnj.net,https://www.bgnj.net,https://scoutkorea-jimmy.github.io"
SESSION_TTL_SECONDS = "2592000"
# 이 이메일로 회원가입하면 자동으로 is_admin=1로 등록된다 (한 번만 가능 — 두 번째 가입 시도는 409).
# 테스트용 placeholder. 이미 admin 등록 완료 — 부트스트랩 비활성 상태.
# v00.125 결정: 평문 유지. 자격 증명이 아니고 비밀번호 보호.
ADMIN_BOOTSTRAP_EMAIL = "admin@admin.admin"
# 슈퍼 관리자 — 콤마로 구분된 이메일은 로그인/세션 조회 시점에 항상 is_admin=1, grade_id='admin' 으로 강제된다.
# 마스터 운영 이메일 (사용자 본인). v00.125 결정: 평문 유지. Secrets 이관 미진행.
SUPER_ADMIN_EMAILS = "scoutkorea@kakao.com"
