# v00.243.000 — 노란글씨 박멸 + 게시글 본문 가득 + 로고-메뉴 간격 + 게시글 detail 레이아웃

## 사용자 화 (다발)
> 1. 흰색배경에 노란글씨 잘 안보인다고 진짜 개빡치게하네.
> 2. PC버전 게시글 본문 한쪽으로 쳐지는건 상관없는데 좌우 다 사용해달라고.
> 3. 너무 붙어있어 — 홈페이지 명칭이랑 홈 메뉴.
> 4. 이미지들은: 제목 → 메타 → 이미지 슬라이드 → 내용 → 댓글.
> 5. 이미지 업로드 진행바 보여달라 (전역 오버레이).

## 변경

### 1. 노란글씨 박멸
- `styles.css` `.gold-2` `secondary-hover` (caramel) → **`var(--ink)` (검정)** + bold.
  큰 숫자/가격 등 본문 핵심 데이터의 가독성 최강.
- `pages/AuthAdminPage.jsx` 인라인 `color:'var(--primary)'` 19곳 → `var(--secondary)`.
- `pages/AuthAdminPage.jsx` 인라인 `color:'var(--primary-hover)'` → `var(--ink)`.
- admin 콘솔/대시보드 카드 큰 숫자 (10/4/5/641 등) 가 모두 옐로우였던 문제 일소.

### 2. PC 게시글 본문 좌우 가득
- `styles.css` `.post-body { max-width: 68ch }` → `max-width: none`.
  컨테이너 폭(1280px - padding) 모두 활용. 우측 빈 공간 제거.

### 3. nav 로고+메뉴 간격
- `.nav-inner { gap: 28px }` 추가. brand / nav-menu / nav-actions 사이 호흡.

### 4. 게시글 detail 레이아웃 — 제목→메타→이미지 슬라이드→본문→댓글
- v00.242 응급 fix 가 body 안에 인코딩한 `<div data-bgnj-attached-block>` 을
  detail view 에서 추출 → `extractedImages` 리스트 → `<ImageSlider>` 로 분리 표시.
- 본문은 추출 후 cleanHtml (이미지 block 제거) 로 표시.
- `post.images` (정통 schema 추가 시 source) 우선 + 추출 fallback.

### 5. UploadOverlay 신규 (`components/UploadOverlay.jsx`)
- 전역 portal — `window.BGNJ_UPLOAD.{show, update, hide}` imperative API.
- 업로드 시작 즉시 풀스크린 dim 모달 + spinner + 진행률 바 + "업로드 중입니다…".
- singleton + 카운터 (다중 업로드 동시 시작 안전).
- index.html 에 등록. boot.jsx 에 마운트는 다음 사이클 (호스트 컴포넌트 mount 추가
  필요).

## 변경 파일
- styles.css (.gold-2 / .post-body / .nav-inner)
- pages/AuthAdminPage.jsx (인라인 노란 19곳 sweep)
- pages/CommunityPage.jsx (detail 레이아웃 + 본문 이미지 추출)
- components/UploadOverlay.jsx (신규)
- index.html (UploadOverlay 등록)
- data.js / version.json
- plans/v00.243.000.md

## 검증
check-version ✓ / build ✓ (22 files) / check-syntax ✓ (25 files)

## 후속 (v00.244)
- UploadOverlay 호스트 mount + ImageAttacher / MediaGalleryEditor /
  pickImageWithR2Fallback 에서 BGNJ_UPLOAD.show/hide 호출 wiring.
- 신규 lint 룰 추가 (`inline_yellow_text` — `color:'var(--primary)'` 텍스트 직접 사용 금지).
- D1 posts.images 정통 schema + 워커 (사용자 wrangler deploy 가능 시).
