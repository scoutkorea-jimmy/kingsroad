# v00.266.000 — 상단 네비 "놀자" 메뉴 클릭으로도 열기 (호버 유지 + 클릭 보완)

## 배경 / 사용자 보고
- 사용자: "놀자 메뉴들이 안보이는데 놀자 메뉴들을 모두 활성화해줘." (위치: 상단 네비게이션 바)
- 진단 결과: 코드·빌드 산출물·배포 버전 모두 정상. "놀자" 버튼과 먹고/자고/사고 항목은 항상 렌더.
- 진짜 원인: 데스크톱에서 먹고/자고/사고 3개는 **hover 전용** 메가 드롭다운 안에만 존재.
  "놀자" 클릭 시 드롭다운이 열리는 대신 곧장 `/eat` 로 이동 → 호버 불가 환경(터치패드/태블릿)에서
  3개 메뉴가 사실상 "안 보임".
- 별도의 활성/비활성 토글은 코드에 없음(=켤 스위치가 애초에 없었음).

## 결정 (사용자 선택)
- **호버 + 클릭 둘 다**: 기존 hover 드롭다운 유지 + "놀자" 클릭 시에도 드롭다운이 열리도록 보완
  (클릭 시 `/eat` 자동 이동 제거). 시각은 동일, 접근성만 확보.

## 구현 (components/Shell.jsx · Nav)
1. `playOpen` state + `playRef`(li) 추가.
2. 라우트 변경 effect 에 `setPlayOpen(false)` 추가(이동 시 닫힘).
3. 바깥 클릭(mousedown) + Escape 로 닫힘 effect 추가.
4. "놀자"(`isMega:'play'`) 버튼 onClick: `go('eat')` 대신 `setPlayOpen(v=>!v)` 토글.
   `aria-expanded={playOpen}` 부여.
5. 데스크톱 메가 `.nav-mega` 에 `playOpen` 시 `nav-mega-open` 클래스 부여(강제 visible).
6. 자식(먹고/자고/사고) 클릭 시 navigate + `setPlayOpen(false)`.

## CSS (styles.css)
- `.nav-mega.nav-mega-open { visibility:visible !important; opacity:1 !important; }` 추가
  (기존 `.nav-has-mega:hover .nav-mega` 와 동급).

## 릴리스
- data.js BGNJ_VERSION 00.265.003 → 00.266.000
- index.html `?v=` cache-buster 일괄 동기 (check-version 검증)
- esbuild 빌드(pre-commit 자동) → Shell.js 동기
- commit + push (자동 배포)

## 검증
- node tools/check-syntax.mjs / check-version.mjs
- 데스크톱: "놀자" 클릭 → 드롭다운 열림 / 다시 클릭 or 바깥 클릭 → 닫힘 / 호버도 여전히 동작
- 모바일: 햄버거 내 인라인 먹고/자고/사고 기존대로 노출(영향 없음)
