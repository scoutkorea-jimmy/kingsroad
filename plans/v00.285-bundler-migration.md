# v00.285 — 번들러 전환 (스파게티 구조 근본 해소)

## 배경 (피드백)
"홈페이지가 스파게티 코딩" 피드백. 코드 직접 진단 결과 — 폴더/페이지 단위 정돈은 돼 있으나
**체감 주범은 모듈 시스템의 부재**다.

### 진단 (실측)
| 신호 | 실측 |
|---|---|
| 번들러/모듈 없음 | `tools/build.mjs` 는 per-file `esbuild.transform` → IIFE 래핑. **번들링 안 함** |
| 전역 네임스페이스 결합 | `window.X = X` 내보내고 `const X = window.X` 받기 — 전체 **1,100회+** (AuthAdminPage.jsx 453회) |
| 거대 단일 파일 | `pages/AuthAdminPage.jsx` 9,273줄 / 선언 452개 |
| 빌드 산출물 중복 커밋 | `.jsx` + `.js` 둘 다 git 커밋 → 모든 PR diff 2배 |
| 수동 캐시버스팅 | `index.html` script 20여 개 `?v=` 수동 동기 |

### 근본 원인
파일 간 의존성을 `import` 대신 **전역 + 로딩순서**로 표현 → (1) 의존 그래프 불투명,
(2) `index.html` 줄 순서가 숨은 의존성, (3) `const X = window.X` 보일러플레이트 범람.

## 목표
per-file IIFE transpile + 전역 네임스페이스 → **단일 엔트리 import/export 번들**.
React/ReactDOM/Tiptap 은 CDN external 유지. GitHub Pages 배포·CSP 무중단.

## 제약 (반드시 보존)
- **배포**: GitHub Pages, `.github/workflows` 가 `path: .` 로 레포 전체 업로드. 도메인 `bgnj.net`.
  → 번들 산출물이 **배포 시점에 존재**해야 함. CI 에 빌드 스텝 추가가 전제.
- **CSP**: 인라인 script sha256 해시 의존 (`tools/csp-hashes.mjs` 자동화 존재). 번들 후 재생성.
- **의도된 런타임 전역 API (남긴다, 모듈화 대상 아님)**:
  `window.BGNJ_API` · `window.BGNJ_TOAST` · `window.BGNJ_VERSION` · `window.ADMIN_*` 데이터 + Tiptap importmap.
- **pre-commit 훅**: stamp-datetime → csp-hashes → check-version → build → check-syntax. 5단계 순서 유지/조정.
- **데이터 손실 안티패턴 금지** (메모리): `(data||[]).map()` 금지, Array.isArray 가드.

## 핵심 전략 — 점진적 공존 (leaf→root, 무중단)
빅뱅 금지. esbuild **bundle 모드**로 전환하되, 미변환 파일과 공존하도록 모듈을 하나씩 전환:
- 변환된 모듈은 전환기 동안 **`export` + `window.X = X` 병행** (구 소비자 호환).
- 소비 측은 의존 모듈이 변환되면 `import`, 아직이면 `window.X` 폴백.
- 의존 없는 leaf(`components/`)부터 → `pages/` → `pages/admin/` → `boot`.
- 각 단계가 독립 배포 가능 + 롤백 가능.

## 단계

### Stage 0 — 안전망 / 베이스라인
- [ ] 현재 동작 스냅샷: 주요 라우트 수동 체크리스트(홈/커뮤니티/투어/강연/한켠/관리자 로그인) — `verify` 스킬 활용.
- [ ] 번들 산출 크기/로드 시간 기준선 기록 (회귀 비교용).
- [ ] 남길 전역 API 목록 확정 (위 제약).

### Stage 1 — 번들 파이프라인 구축 (코드 의미 변경 없음) ✅ 완료 (branch: feat/v00.285-bundler-stage1)
- [x] `tools/build.mjs`: `--bundle` 모드 추가 (per-file transform 과 병행, 롤백 안전). iife, jsxFactory=React.createElement, target es2018.
- [x] 엔트리 2개 신설 — `src/entry-main.jsx` (index.html 순서) + `src/entry-admin.jsx` (ADMIN_SCRIPTS 순서). **코드 스플리팅 경계 보존.**
- [x] React/ReactDOM/DOMPurify/Tiptap = 소스에서 import 안 함(전역 자유변수) → external 설정 불필요. esbuild 가 그대로 둠 확인.
- [x] CI: `.github/workflows/bundle-check.yml` 신설 — PR/push 마다 번들 빌드 + `node --check` 검증. **배포(deploy-pages.yml)는 미변경** → 무영향.
- [x] `dist/` gitignore, `npm run build:bundle` 스크립트 추가.

**검증 결과 (정적 동등성):**
- 빌드 clean (esbuild 경고/에러 0), `node --check` app.js·admin.js 통과.
- 전역 노출 surface: 기존 per-file 56개 vs 번들 55개 — 차이는 `window.BGNJ_UPLOAD` 1개뿐.
- **발견(죽은 코드):** `components/UploadOverlay.jsx` 는 index.html·boot·admin 어디서도 로드 안 됨 + `BGNJ_UPLOAD.show/hide` 소비처 0. v00.243 plan 의 wiring 미완결. 번들의 "누락"은 회귀 아님 — 오히려 실제 로드 대상만 정확히 포함. → Stage 5 에서 UploadOverlay 제거 검토.
- `Object.assign(window,{AdminPage,...})` admin.js 보존, `ReactDOM.createRoot` app.js 보존, BGNJ_API/TOAST/VERSION/LOAD_ADMIN 전역 보존.

**남은 검증 (Stage 3 진입 전):** 브라우저 런타임 스모크 — 번들 가리키는 index 사본을 프리뷰 배포에서 실제 렌더 확인 (정적 동등성은 끝났으나 런타임 확인은 swap 단계와 함께).

### Stage 2 — 모듈 전환 (leaf→root, 반복)
- [ ] `components/*` : `window.X = X` → `export`, 소비처 `import`. (KoreaMap, MediaGallery, Tiptap, ConfirmDialog 등)
- [ ] `pages/*` : 동일.
- [ ] `pages/admin/*` + 9,273줄 `AuthAdminPage.jsx` 는 이 시점에 **도메인 분할 병행**(Auth/Privacy/ReportQueue/Dashboard).
- [ ] `boot.jsx` 엔트리화. 전역 폴백(`window.X`) 제거.
- 각 모듈 전환 후 해당 라우트 verify.

### Stage 3 — index.html → 번들 전환 ✅ 완료 (branch: feat/v00.285-bundler-stage1)
- [x] index.html: per-file `<script defer>` **22개 → 단일 `dist/app.js`** 1개.
- [x] boot.jsx `ADMIN_SCRIPTS`: admin 5개 per-file → 단일 `dist/admin.js` (코드 스플리팅 경계 유지).
- [x] `deploy-pages.yml`: 업로드 전 Node setup + `npm run build:bundle` + 번들 존재 검증. (dist/ 는 gitignore → CI 가 생성.)
- [x] CSP: 인라인 script 미변경(외부 script 만 교체) → sha256 해시 재생성 불필요. 콘솔 CSP 위반 0 확인.
- 캐시버스팅: 현재 `?v=00.284.004` 유지(check-version 훅 호환). 해시 파일명 자동화는 후속 과제로 이월.

**런타임 검증 (헤드리스 Chrome, 실제 index.html):**
- 홈: `dist/app.js` 부팅 → `#root` app 마운트(56KB DOM), 콘솔 에러/CSP 위반 0.
- admin(`#/admin`): route 진입 시 `dist/admin.js` 동적 GET 200 → AuthAdminPage 로그인 UI 렌더. **비-admin 은 admin.js 미요청** = 스플리팅 보존.
- ⚠️ 프로덕션 반영은 **main 머지 시점**에만 (배포 워크플로 trigger = push main). 브랜치는 무영향.

### Stage 4 — 빌드 산출물 git 추방 ✅ 완료 (branch: feat/v00.285-bundler-stage1)
- [x] 커밋된 빌드 산출물 **25개 `.js` 추방** (`.jsx` 짝이 있는 것만). 손작성 소스 3개(`api.js`/`data.js`/`components/KoreaMapData.js`)는 **보존**.
- [x] `tools/build.mjs`: per-file transform 경로 **제거**, 기본 동작 = 번들. `--watch` 는 esbuild context watch 로 번들 감시.
- [x] pre-commit 훅(install-hooks.sh) 재작성: build 스텝 = 번들 빌드(실패 시 commit 차단), per-file `.js` 자동 stage **제거** (dist/ gitignore 라 불필요). 활성 훅 재설치.
- [x] `tools/package.json`: `build`(번들) / `build:watch` 스크립트.
- [x] 검증: `node build.mjs` 번들 정상, check-syntax 28파일(25 jsx + 3 손작성 js) clean.
- **효과:** 매 PR diff 에서 산출물 .js 중복 소멸(리뷰 노이즈 절반↓).
- ⏸ **버전 범프 + ADMIN_VERSION_HISTORY 엔트리**: 릴리스(main 머지) 시점에 추가 — version.json ↔ index.html `?v=` ↔ BGNJ_VERSION 일관성(check-version 훅)과 함께. 현재 `?v=00.284.004` 유지.

### Stage 5 — 문서 현행화
- [ ] CONTEXT.md / README.md / ai-development-rules.md 빌드·아키텍처 절 갱신.
- [ ] `AdminDesignHub.jsx` 디자인/아키텍처 가이드 갱신 (살아있는 문서 원칙).
- [ ] ROADMAP.md 반영.

## 리스크 & 완화
- **배포 중단**: Stage 1 에서 CI 빌드 스텝을 먼저 검증(브랜치 배포/프리뷰). 산출물 커밋 제거(Stage 4)는 CI 빌드 안정 확인 후.
- **CSP 위반(흰 화면)**: 번들 후 즉시 `csp-hashes.mjs`, 콘솔 CSP 에러 0 확인.
- **전역 API 회귀**: BGNJ_* 는 모듈화 제외, 엔트리에서 명시적으로 window 부착.
- **대규모 기계 변환 오류**: codemod 일괄 대신 leaf→root 모듈 단위 + 라우트별 verify.

## 진행 순서 결정 사항 (사용자 확인 필요)
1. Stage 1(파이프라인)부터 별도 PR/브랜치로 착수할지.
2. `AuthAdminPage.jsx` 분할을 Stage 2 와 함께할지, 선행 분리할지.
