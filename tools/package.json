{
  "name": "bgnj-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Local-only tooling for the BGNJ repo (syntax check, hooks). Not shipped.",
  "type": "module",
  "scripts": {
    "check": "node check-syntax.mjs",
    "build": "node build.mjs",
    "build:bundle": "node build.mjs --bundle",
    "install-hooks": "bash install-hooks.sh"
  },
  "dependencies": {
    "@babel/parser": "^7.25.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
