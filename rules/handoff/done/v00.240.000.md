# v00.240.000 — 홈 칼럼 자동 순환 (최근 5개)

## 사용자 요청
> 홈 칼럼은 자동으로 순환되게 해줘. 좌측에 큰 메인 칼럼은 최근에 올라는 5개
> 칼럼들 중심으로.

## 변경 (`pages/HomePage.jsx`)

- `recentFiveColumns = publicColumns.slice(0, 5)` — 최근 5개.
- `featuredIdx` state. 5초 간격 `setInterval (idx + 1) % length`.
- pause: hover (`onMouseEnter/Leave`) + focus (`onFocusCapture/BlurCapture`).
  접근성 — 키보드 포커스 시도 자동 이동 정지.
- `secondaryColumns` 는 메인 idx 제외한 4개 (순서 유지).
- 카로셀 인디케이터 (점 N개, 활성은 22×8 옐로우 알약 / 비활성은 8×8 line-2):
  - `aria-label`, `aria-current` 접근성.
  - 클릭 시 setFeaturedIdx (수동 이동).
  - "▶ AUTO" / "⏸ HOVER" mono 라벨로 상태 시각화.
- 칼럼 1개 이하면 인디케이터 미노출 + 자동 순환 비활성.

## 변경 파일
- `pages/HomePage.jsx`
- `data.js` / `index.html` (21곳) / `version.json`
- `plans/v00.240.000.md`

## 검증
- check-version ✓ / build ✓ / check-syntax ✓

## 후속 (다음 사이클)
- 사용자 보고 "새로고침 시 과거 텍스트/이미지 깜빡" — FOUC 진단.
- 게시판 이미지 안 보임 (Tiptap toolbar 점검).
