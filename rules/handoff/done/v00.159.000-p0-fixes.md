# v00.159.000 — P0 3건 일괄 fix (LecturesPage 빈 버킷 / nav 폴백 / helper 직접 호출)

> 작업 접수: 2026-05-04 ("다음 사이클 진행" — v00.156 검토 보고서의 P0 3건)
> 직전 commit: `9b9c1d5` v00.158.000 (버튼 모양 자연스럽게)

## 1. 사용자 요청 (원문)

> "다음 사이클 진행"

직전 답변에서 명시한 v00.159 후보 = v00.156 사이트 검토 보고서의 P0 3건.

## 2. 변경 대상 파일

| 파일 | 변경 |
|---|---|
| `pages/LecturesPage.jsx` | line 53-56 — `lectures.length === 0` 일 때 `lecture = undefined` → `lecture.id` throw. `if (!lecture) return <안내>` 가드 |
| `components/Shell.jsx` | line 363 nav 폴백 `"책"` → `"뱅기노자 도서"` (다권화 라벨 정합) |
| `pages/AuthAdminPage.jsx` | line 5996 주석 `왕의길 (책 주문 운영)` → `도서 주문 운영` (남은 옛 라벨 잔재 정리) |
| `pages/LegalFaqPages.jsx` | `window.BGNJ_LEGAL.get` / `window.BGNJ_FAQ.listCategories/search` 직접 호출 4곳 → `G.call/.arr` 가드. `G = window.BGNJ_GUARD \|\| 인라인 폴백` 도입 |
| `pages/BookCheckoutPage.jsx` | BookReviewSection useState 초기화 (line 5 `useState(() => window.BGNJ_BOOK_ORDERS.listReviews())`) → 안전 폴백. 모듈 상단 `G` 도입 |
| `pages/admin/AdminDesignHub.jsx` | ADMIN_VERSION_HISTORY v00.159 entry |
| `data.js` + `index.html` | v00.158 → v00.159 + cache-buster |

## 3. 구현 단계

1. **P0-1** LecturesPage — `lecture` undefined 가드. `lectures.length === 0` 면 빈 안내 카드 (현재는 line 110 안내문구가 있지만 그 위 line 55 가 먼저 평가됨).
2. **P0-2-a** Shell.jsx nav 폴백 한 줄.
3. **P0-2-b** AuthAdminPage 주석 한 줄.
4. **P0-3-a** LegalFaqPages — 모듈 상단에 `const G = window.BGNJ_GUARD \|\| { call: (fn, fb) => { try { return fn() ?? fb; } catch { return fb; } }, arr: (fn) => { try { const v = fn(); return Array.isArray(v) ? v : []; } catch { return []; } } };` 추가. 4곳 직접 호출을 G.call/G.arr 로.
5. **P0-3-b** BookCheckoutPage — BookReviewSection 의 useState 초기 호출을 `G.arr` 로 안전화. 마찬가지로 모듈 상단에 G 폴백 (이미 import 됨 체크). 그 외 inline rendering 시점 helper 호출은 lazy callback 안이라 risk 낮음 — 본 사이클은 mount-time throw 만 차단.
6. 빌드 → commit → push.

## 4. 검증 기준

- [ ] LecturesPage: 강연 1개 startsAt 미래로 두고 '지난 강연' 탭 → 빨간 PageErrorBoundary 카드 대신 정상 안내 (강연 0건 안내).
- [ ] /book 진입: 헤더 메뉴 라벨 "뱅기노자 도서" (D1 site_content_kv 가 비어있어도 폴백이 옳은 텍스트).
- [ ] dev tools 에서 `delete window.BGNJ_LEGAL` 후 /privacy 진입 → 흰 화면 대신 빈 카드 안내.
- [ ] dev tools 에서 `delete window.BGNJ_BOOK_ORDERS` 후 /book 진입 → 리뷰 섹션이 빈 상태로 표시 (페이지 전체는 정상).
- [ ] 빌드 / check-version / check-syntax 통과.

## 5. 비대상 (별 사이클)

- MyPage 의 lazy callback 안 helper 직접 호출 — 표준 패턴 정합 차원. 다음 a11y/성능 사이클에서 같이.
- AuthAdminPage 의 BGNJ_COMMUNITY 직접 호출 — 같은 이유.
- D1 site_content_kv 의 nav.book 옛 row 정리 — 사용자 운영 작업 (admin → 사이트 콘텐츠 → 메뉴).
- P1 (a11y / 성능 / PII) — v00.160+ 별 사이클.

## 6. 남은 위험

- LegalFaqPages 의 G 폴백 도입 시 inline 객체가 매 렌더 새로 생성 — 의존성 영향 0 (G 자체를 deps 로 안 씀). OK.
- BookReviewSection 가드 후 listReviews 가 `[]` 폴백 → 리뷰 표시 안 되지만 페이지 mount 는 정상. helper 가 늦게 도착하면 `bgnj-orders-refresh` 또는 component remount 으로 재계산 필요 — 이미 listReviews 가 setReviews 로 갱신되는 별도 트리거 없음. 후속에서 useEffect 로 helper-ready 감시 추가 검토.
