# v00.239.000 — 포스터 가득 / 칩 KMS 정합 / 줄바꿈 / 주관 / nav 슬라이드 / 업로드 spinner

## 사용자 보고 6+ 건 일괄

1. 포스터는 비율 구애없이 가로 가득 + 자연 비율.
2. 칩 컬러 디자인 규칙 정합 검토.
3. 강연 제목/주제 줄바꿈.
4. 주최/주관 작성 가능.
5. 좌측 로고/우측 이름 1줄, 가운데는 슬라이드.
6. 업로드 중 동적 피드백.

## 변경

### 포스터 — `pages/LecturesPage.jsx` / `pages/WangsanamTourPage.jsx`
- v00.238 의 `flex+contain+maxHeight:'70vh'` 박스 제거.
- `<img style={{width:'100%', height:'auto', display:'block'}}/>` — 컬럼 가득
  좌우 여백 0 + 자연 비율.

### 칩 KMS 정합
- v00.238 `info(blue)` 시스템 색 제거 (KMS §2 위배).
- 4단계: primary 옐로우(브랜드) / secondary caramel(주최) / secondary-hover(주관) /
  tertiary slate(장소·규모) / neutral ink-2(결제).

### 줄바꿈
- detail h2/note `whiteSpace:'pre-wrap'`.
- 모달 input → textarea (Enter 줄바꿈).

### 주관(organizer)
- `site_content_kv.lecturePages[id].organizer` — 워커 deploy 회피.
- 모달 input + detail 칩 (secondary-hover 톤).

### nav — `styles.css`
- `.nav-menu` `flex: 1 1 0 + min-width: 0 + overflow-x: auto` (PC 포함 항상
  슬라이드, 사용자 명시 "지금처럼"). `flex-wrap: nowrap`.
- `.brand`, `.brand > *` `nowrap + flex-shrink: 0`.
- `.nav-actions`, `.nav-actions > *`, `.nav-actions button` 동일.

### 업로드 spinner — `components/MediaGallery.jsx`
- drop zone 안 CSS spinner (primary 옐로우 ring, `@keyframes bgnj-spin`)
  + "사진 업로드 중 N / M" + 진행률 바 (smooth transition) + "R2 업로드 — 잠시만
  기다려 주세요" 안내.

## 게시판 이미지 진단 (별도 사이클)
- DOMPurify ALLOWED_TAGS img / ALLOWED_ATTR src 모두 OK.
- CSP img-src https/data/blob 모두 OK.
- Tiptap `Image.configure({ inline:false, allowBase64:true })` 설치 OK.
- 원인 후보: PostCompose 의 toolbar 에 이미지 추가 UI 부재 또는 paste/drop 핸들러
  미구현. v00.240 에서 점검.

## 사이트 설정 페이지별 타이틀
이미 SiteContentAdminPanel 에 `lectureIntro/tourIntro/columnIntro/faqIntro/checkoutIntro`
의 `titlePrefix/titleAccent/titleSuffix` 편집 가능. 사용자 인지 안내만.

## 검증
- check-version ✓ / build ✓ (21 files) / check-syntax ✓ (24 files)
