# v00.152.000 — 홈 책 CTA 다권 카루셀 + hero 지도 버튼 삭제

> 작업 접수: 2026-05-03 (사용자 "남은 작업 진행" 지시)
> 사이클 직전 commit: `e5cb0dd` v00.151.000 (홈 책 CTA + BookPage 표지 실 데이터 사용)
> 본 사이클 commit (예정): TBD

## 1. 사용자 요청 (원문)

> "남은 작접 진행하고, 작업이 접수되면 무조건 md파일 만들어서 작업계획을 수립하는것을 작업 프로세스에 꽂아놔
> 그리고 에러 페이지들의 에러 항목들을 테스트해보고 싶은데 어떻게 하면 할 수 있지?
> 그리고 모든 명령을 넣으면 오류 로그를 파악하고 오류를 해결하는것을 가장 최 우선으로 업무 가이드에 넣어놔"

본 plan 의 직접 범위는 **첫 줄 "남은 작업 진행"** = working-tree 에 이미 있던 v00.152.000 미완 변경 마무리. 나머지 두 항목(plan-md 룰 / 오류 우선 룰)은 메타 작업 가이드 등록(별도) + 에러 페이지 테스트 방법은 안내 답변(별도).

## 2. 미완 상태 (working tree 진단)

`pages/HomePage.jsx:781` 에서 `<BookCarouselSection go={go} dataTick={dataTick}/>` 호출. **컴포넌트 정의 0건** → 빌드 시 ReferenceError.
함께 적용된 변경:
- 버전 `00.151.000` → `00.152.000` (data.js + index.html cache-buster 17 곳)
- HomePage hero CTA 의 "지도에서 여행지 찾기" 버튼 삭제, 옆 "커뮤니티 참여하기" 가 `btn-gold` 로 격상

## 3. 변경 대상 파일

| 파일 | 변경 |
|---|---|
| `pages/HomePage.jsx` | `BookCarouselSection` 컴포넌트 신규 정의 (HeroProgramCards 직후) — primary 우선 정렬, 좌우 무한 wrap, autoplay 7s, hover 일시정지, 책 1권이면 chrome hide, 0권이면 섹션 자체 hide |
| `pages/HomePage.js` | esbuild 산출물 — pre-commit 자동 생성 |
| `pages/admin/AdminDesignHub.jsx` | `ADMIN_VERSION_HISTORY` 맨 앞에 v00.152.000 entry |
| `data.js` / `index.html` | (이미 적용됨) 버전 + cache-buster |

## 4. 구현 단계

1. `BookCarouselSection` 작성. 데이터 소스: `window.BGNJ_BOOKS.list({ status:'published' })`. 정렬: primary → order. `bgnj-books-refresh` event + `dataTick` prop 양쪽 청취하여 admin 변경 즉시 반영.
2. UI: 기존 v00.151 단일 책 IIFE 의 `cta-grid 1fr 1fr` 좌우 카드 디자인 그대로 1슬라이드로 재사용. 책마다 title / desc / 가격(KR/EN, 미입력 hide) / 표지(coverDataUri 있으면 img, 없으면 generic placeholder) / 구매 버튼 → `go('book')`.
3. 카루셀 chrome (책 ≥ 2): 양옆 원형 ‹/› 버튼, 하단 dot indicator (현재 idx 는 폭 24, 나머지 8). hover 시 autoplay 일시정지.
4. 좌우 무한: `(idx + N) % N` modulo wrap, 끝에서 prev / 처음에서 next 모두 자연스럽게 순환.
5. 책 0권: `return null` — 섹션 자체 미노출.
6. ADMIN_VERSION_HISTORY 신규 entry — datetime sentinel `new Date().toISOString()` (pre-commit stamp 가 KST ISO 로 치환).
7. esbuild 미리 실행해서 빌드 에러 사전 파악 (룰: 명령 출력 오류 최우선). 깨끗하면 git add → commit.
8. pre-commit (stamp / csp / check-version / build / check-syntax) 통과 확인. 차단되면 root cause 수정 후 재시도.
9. push (auto deploy = GitHub Pages).

## 5. 검증 기준

- [ ] 홈 페이지 콘솔에 ReferenceError 없음
- [ ] 책 1권일 때: 단일 카드 표시, ‹/› 버튼·dot indicator 미노출
- [ ] 책 2권 이상: 좌/우 버튼·dot 노출, 7초 autoplay, hover 시 정지, 끝에서 prev → 마지막 책으로 순환
- [ ] admin 에서 책 추가/삭제/순서 변경 → 홈 새로고침 없이 즉시 반영 (`bgnj-books-refresh` event)
- [ ] 가격 0/null 인 판본은 카드에서 hide
- [ ] 표지 미업로드 책은 generic placeholder
- [ ] 모든 책 published=draft 면 섹션 자체 hide
- [ ] hero CTA 영역에 "지도에서 여행지 찾기" 버튼 사라진 상태로 "커뮤니티 참여하기" btn-gold 단독

## 6. 배포·커밋 메모

- pre-commit hook 이 자동 처리: stamp-datetime / csp-hashes / check-version / build (esbuild) / check-syntax
- commit 메시지: `feat: 홈 책 CTA 다권 카루셀 + hero 지도 버튼 제거 (v00.152.000)`
- push → GitHub Pages 자동 배포

## 7. 남은 위험

- 책이 매우 많을 때 (>10권) dot indicator 가 길어짐 — v00.152 범위 밖, 발견 시 next 사이클에서 압축
- `<HomeSectionBoundary>` 내부에서 hook(useState/useEffect) 사용 — 컴포넌트는 함수 컴포넌트라 정상. 단, ErrorBoundary 가 자식 throw 시 fallback. autoplay setTimeout 은 cleanup 정상.
- autoplay 7s 가 사용자 독서 시간을 끊을 수 있음 — pause-on-hover 로 완화. 추후 필요 시 prefers-reduced-motion respect 추가 검토.
