# v00.288.003 — 홈 커뮤니티 섹션 제거

## 지시
사용자: "메인화면에 얘는 빼줘" (홈 화면 커뮤니티 섹션 "다녀온 사람들의 기록" 스크린샷 첨부).

## 변경
- `pages/HomePage.jsx`
  - 커뮤니티 섹션(`<HomeSectionBoundary label="커뮤니티">` ~ `</section></HomeSectionBoundary>`, 69줄) 제거.
  - 해당 섹션 전용이던 `recentPosts` useMemo 제거. `postsTick` 은 `dataTick` 합산에 여전히 쓰이므로 유지.
- `data.js` / `index.html` / `version.json` — v00.288.002 → v00.288.003 동기.
- `dist/app.js` · `dist/admin.js` 재빌드.
- `.gitignore` — `.superpowers/` (브레인스토밍 로컬 산출물) · `.playwright-mcp/` 추가.

## 남겨둔 것 (의도)
- `homeText.community*` 기본 문구 8개와 `AdminContentEditors.jsx` 의 "커뮤니티 섹션" 편집 그룹은 **유지**.
  홈 전면 개편이 예정돼 있어 지금 걷어내면 되돌리기 비용만 생김. 개편 시 함께 정리한다.
  → 현재 관리자에서 이 그룹을 편집해도 홈에 반영되는 곳이 없음(알려진 상태).

## 환경 메모
- `tools/node_modules` 의 esbuild 네이티브 바이너리가 깨져 있어 빌드 실패 → `tools/` 에서 `rm -rf node_modules && npm install` 로 복구.
  PC 간 이동 시 재발 가능. 빌드가 "installed esbuild for another platform" 로 죽으면 같은 방법으로 복구.

## 검증
- `node tools/check-syntax.mjs` — 41 files parsed cleanly (차단 룰 0건).
- `node tools/build.mjs` — 2 bundles built.
- `node tools/check-version.mjs` — 통과.
- `node tools/csp-hashes.mjs` — inline script 5개 해시 갱신.
